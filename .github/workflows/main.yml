name: Build Pure GKI Kernel 5.10.218 (AnyKernel3)

on:
  workflow_dispatch:
    inputs:
      manifest_branch:
        description: '谷歌 GKI 分支'
        required: true
        default: 'common-android12-5.10'

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    steps:
      - name: 强力清理磁盘空间
        run: |
          sudo docker image prune -a -f
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup /opt/pipx
          sudo apt-get clean
          df -h

      - name: 安装依赖工具 (包含 ccache)
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential curl git python3 rsync zip ccache
          mkdir -p ~/.bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/.bin/repo
          chmod a+rx ~/.bin/repo
          echo "$HOME/.bin" >> $GITHUB_PATH

      - name: 设置 CCache 编译缓存
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-gki-5.10-${{ github.run_id }}
          restore-keys: |
            ccache-gki-5.10-

      - name: 初始化并同步源码
        run: |
          sudo chown $USER /mnt
          mkdir -p /mnt/kernel_workspace && cd /mnt/kernel_workspace
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          repo init -u https://android.googlesource.com/kernel/manifest -b ${{ github.event.inputs.manifest_branch }} --depth=1
          repo sync -j$(nproc) --no-clone-bundle --no-tags
        

      - name: 注入内核配置并禁用格式检查
        run: |
          cd /mnt/kernel_workspace/common
          cat <<EOF >> arch/arm64/configs/gki_defconfig
          CONFIG_BPF_STREAM_PARSER=y
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
          CONFIG_NETFILTER_XT_SET=y
          CONFIG_IP_SET=y
          CONFIG_IP_SET_MAX=65534
          CONFIG_IP_SET_BITMAP_IP=y
          CONFIG_IP_SET_BITMAP_IPMAC=y
          CONFIG_IP_SET_BITMAP_PORT=y
          CONFIG_IP_SET_HASH_IP=y
          CONFIG_IP_SET_HASH_IPMARK=y
          CONFIG_IP_SET_HASH_IPPORT=y
          CONFIG_IP_SET_HASH_IPPORTIP=y
          CONFIG_IP_SET_HASH_IPPORTNET=y
          CONFIG_IP_SET_HASH_IPMAC=y
          CONFIG_IP_SET_HASH_MAC=y
          CONFIG_IP_SET_HASH_NETPORTNET=y
          CONFIG_IP_SET_HASH_NET=y
          CONFIG_IP_SET_HASH_NETNET=y
          CONFIG_IP_SET_HASH_NETPORT=y
          CONFIG_IP_SET_HASH_NETIFACE=y
          CONFIG_IP_SET_LIST_SET=y
          CONFIG_IP6_NF_NAT=y
          CONFIG_IP6_NF_TARGET_MASQUERADE=y         
          CONFIG_TCP_CONG_BBR=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_DEFAULT_BBR=y
          CONFIG_NET_SCH_FQ_CODEL=y
          CONFIG_NET_SCH_CAKE=y
          CONFIG_IP_ADVANCED_ROUTER=y
          CONFIG_IP_MULTIPLE_TABLES=y
          CONFIG_IP_ROUTE_MULTIPATH=y
          CONFIG_NETFILTER_XT_TARGET_TPROXY=y
          CONFIG_NETFILTER_XT_MATCH_SOCKET=y          
          CONFIG_BPF_JIT=y
          CONFIG_NET_RX_BUSY_POLL=y
          CONFIG_XPS=y
          CONFIG_RPS=y
          EOF
          
          sed -i 's/check_defconfig//g' build.config.gki
          sed -i 's/check_defconfig//g' build.config.common
                    

      - name: 编译内核
        run: |
          export CCACHE_DIR=~/.ccache
          export CCACHE_MAXSIZE=3G
          export CCACHE_EXEC=$(which ccache)   # ← 加在这里，编译命令之前
          ccache -M 3G
          ccache -z

          cd /mnt/kernel_workspace
          LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh

          echo "=== CCache 命中统计 ==="
          ccache -s

      - name: 打包 AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git
          rm -rf AnyKernel3/.git
          IMAGE_PATH=$(find /mnt/kernel_workspace/out -name "Image" | grep "arch/arm64/boot/Image" | head -n 1)
          cp $IMAGE_PATH AnyKernel3/
          
          cd AnyKernel3
          # 核心修复逻辑
          sed -i 's/kernel.string=.*/kernel.string=Router-Monster-GKI/' anykernel.sh
          sed -i 's/do.devicecheck=.*/do.devicecheck=0/' anykernel.sh
          # 强制指定物理路径
          sed -i 's|block=.*|block=/dev/block/by-name/boot;|' anykernel.sh
          # 开启自动分区识别
          sed -i 's/is_slot_device=.*/is_slot_device=auto;/' anykernel.sh
          
          zip -r9 ../AnyKernel3-Monster-GKI.zip *


      - name: 提取并上传产物
        uses: actions/upload-artifact@v4
        with:
          name: AnyKernel3-GKI-5.10.218-NoRoot
          path: AnyKernel3-GKI-5.10.218.zip
